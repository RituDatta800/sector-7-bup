# Base URL of the GridWise LLM NestJS backend.
# Used server-side by the /api/optimize-energy route handler (never exposed to the browser).
GRIDWISE_API_BASE_URL=http://localhost:3000

# Request timeout (ms) for the upstream optimize call. LLM interpretation can be slow.
GRIDWISE_API_TIMEOUT_MS=45000
